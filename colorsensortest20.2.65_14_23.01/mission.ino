/*
void setup(){
  Serial.begin(9600);
  colorsensor_setup();
}
void loop(){
  read_colour();
  switch(count)
  {
    case 0 : delay(5000); count++; Serial.print(count);
    case 1 : Run(90); count++; Serial.print(count);
    case 2 : do {
        read_colour();
        case_yellow();
        if (b_yellow == true) {
          R_delay(1000);
          count++;
          Serial.print(count);
        }
      }
      while (count < 3);
      break;
   case 3 : do{ Press_Limitswitch(); } while(count<4); Serial.print(count);  break;
   case 4 : L_delay(3000); count++; Serial.print(count); break;
}
} */
